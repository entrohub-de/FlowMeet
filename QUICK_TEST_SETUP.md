# 快速测试环境设置

一键创建完整的测试环境，包含8种不同的配对场景。

## 🚀 快速开始（3分钟）

### 步骤1：运行数据库迁移

```bash
cd supabase
supabase db push
```

这会应用所有迁移，包括位置字段的添加。

### 步骤2：运行种子数据

```bash
supabase db reset  # 重置数据库并运行种子数据
# 或
psql $DATABASE_URL < seed.sql  # 只运行种子数据
```

### 步骤3：登录测试

使用以下任一测试账号登录：

```
邮箱：david.zhang@flowmeet.com
密码：password123

邮箱：lisa.wang@flowmeet.com
密码：password123

邮箱：fiona.wang@flowmeet.com
密码：password123
```

### 步骤4：查看配对

1. 登录后访问：`/user/matching`
2. 选择活动：**"配对测试活动 - 正在进行"**
3. 查看"我的配对"标签

---

## 📋 测试场景说明

种子数据已自动创建8种不同的配对场景：

### ✅ 可以测试的场景

| # | 场景 | 状态 | 位置数据 | 测试要点 |
|---|------|------|---------|---------|
| 1 | 完美场景 | accepted | 双方都有（刚刚） | 验证双向位置显示、时间显示"刚刚" |
| 2 | 单方位置 | accepted | 只有对方（5分钟前） | 验证对方位置显示、我的位置输入框 |
| 3 | 空位置 | accepted | 双方都没有 | 验证空状态提示、输入框可用 |
| 4 | 待处理 | pending | 无 | 验证不显示位置功能 |
| 5 | 已完成 | completed | 双方都有（30分钟前） | 验证只读显示（如果实现） |
| 6 | 旧位置 | accepted | 我的位置（2小时前） | 验证时间显示"2小时前" |
| 7 | 中等时间 | accepted | 对方位置（30分钟前） | 验证时间显示"30分钟前" |
| 8 | 已拒绝 | declined | 无 | 验证不显示此配对 |

---

## 🎯 具体测试步骤

### 测试1：查看不同位置状态

**目标：** 验证不同位置数据的显示

1. 登录 `david.zhang@flowmeet.com`
2. 访问 `/user/matching`
3. 选择"配对测试活动"
4. 应该看到多个配对卡片，观察：
   - ✅ 场景1：显示双方位置 + "刚刚"时间
   - ✅ 场景2：只显示对方位置 + "5分钟前"
   - ✅ 场景3：只显示我的输入框，无对方位置
   - ✅ 场景4（pending）：不显示位置部分
   - ✅ 场景6：显示我的位置 + "2小时前"

### 测试2：更新位置

**目标：** 验证位置更新功能

1. 找到场景3（空位置的配对）
2. 在"我的位置"输入框输入：`一楼星巴克门口`
3. 点击"更新位置"按钮
4. 验证：
   - ✅ 按钮显示"更新中..."
   - ✅ 更新成功后输入框清空
   - ✅ （刷新页面）看到我的位置已保存

### 测试3：查看对方位置

**目标：** 验证对方可以看到我的位置

1. 退出登录
2. 用另一个账号登录（如 `lisa.wang@flowmeet.com`）
3. 访问 `/user/matching`
4. 选择"配对测试活动"
5. 找到与david的配对
6. 验证：
   - ✅ 看到对方位置："一楼星巴克门口"
   - ✅ 时间显示"刚刚"或"X分钟前"

### 测试4：时间格式验证

**目标：** 验证不同时间间隔的显示

观察不同配对卡片的时间显示：
- ✅ < 1分钟：显示"刚刚"
- ✅ 1-59分钟：显示"X分钟前"
- ✅ 1-23小时：显示"X小时前"
- ✅ 更早：显示完整时间

### 测试5：边界情况

**目标：** 验证输入限制和错误处理

1. 在输入框中输入超长文本（>100字符）
2. 验证：
   - ✅ 输入框限制100字符
   - ✅ 超出部分无法输入

3. 输入空格后点击更新
4. 验证：
   - ✅ 按钮保持禁用状态

5. 输入有效位置后点击更新，立即再次点击
6. 验证：
   - ✅ 按钮在更新中时禁用

---

## 🗂️ 测试数据概览

### 创建的测试用户（前6个用于配对测试）

| 昵称 | 邮箱 | 性别 | 年龄段 |
|------|------|------|--------|
| 张伟 (David) | david.zhang@flowmeet.com | 男 | 25-34 |
| Lisa Wang | lisa.wang@flowmeet.com | 女 | 25-34 |
| 王芳 (Fiona) | fiona.wang@flowmeet.com | 女 | 35-44 |
| Alex Liu | alex.liu@flowmeet.com | 男 | 45+ |
| Emily Chen | emily.chen@flowmeet.com | 女 | 18-24 |
| Kevin Yang | kevin.yang@flowmeet.com | 男 | 25-34 |

### 创建的测试活动

| 活动名称 | 状态 | 签到码 | 配对场景数 |
|---------|------|--------|-----------|
| 配对测试活动 - 正在进行 | 进行中 | 888888 | 8个 |
| Python 编程入门工作坊 | 未开始 | 随机 | 0 |
| 产品经理交流会 | 未开始 | 随机 | 0 |
| ...更多活动... | ... | ... | ... |

---

## 🛠️ 重置测试环境

如果需要重新开始测试：

```bash
# 完全重置数据库并重新创建种子数据
supabase db reset

# 或只删除配对测试数据
psql $DATABASE_URL -c "
  DELETE FROM evt_matches WHERE event_id IN (
    SELECT event_id FROM evt_events WHERE name LIKE '%配对测试%'
  );
"
```

---

## 📸 预期效果截图说明

登录后在配对页面，你应该看到类似这样的卡片：

```
┌────────────────────────────────────┐
│ 👤 Lisa Wang                       │
│ 女 25-34                           │
│ 🟢 已接受                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 📍 对方位置                        │
│   二楼休息区沙发，穿红色连衣裙      │
│   刚刚                             │
│                                    │
│ 我的位置                           │
│ [输入框: 如：咖啡区...] [更新位置] │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ 👤 王芳 (Fiona)                    │
│ 女 35-44                           │
│ 🟢 已接受                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 📍 对方位置                        │
│   主会场入口，靠近签到台...         │
│   5分钟前                          │
│                                    │
│ 我的位置                           │
│ [输入框: 如：咖啡区...] [更新位置] │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ 👤 Alex Liu                        │
│ 男 45+                             │
│ 🟢 已接受                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 我的位置                           │
│ [输入框: 如：咖啡区...] [更新位置] │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ 👤 Emily Chen                      │
│ 女 18-24                           │
│ 🟡 待确认                          │
│                                    │
│ [✓ 接受] [✗ 拒绝]                  │
└────────────────────────────────────┘
```

---

## ❓ 常见问题

### Q: 运行seed.sql后看不到配对？

**A:** 确保：
1. 已运行数据库迁移（`supabase db push`）
2. 使用正确的测试账号登录
3. 选择了"配对测试活动 - 正在进行"
4. 查看"我的配对"标签页

### Q: 位置更新失败？

**A:** 检查：
1. 浏览器控制台的错误信息
2. 数据库迁移是否成功（位置字段是否存在）
3. 用户是否有更新权限（RLS策略）

### Q: 想要更多测试数据？

**A:** 修改 `seed.sql` 文件中的配对创建部分，增加更多场景。

---

## 🎉 下一步

测试完成后，你可以：

1. ✅ 验证所有UI组件正常工作
2. ✅ 测试不同浏览器的兼容性
3. ✅ 添加Realtime实时同步（可选优化）
4. ✅ 添加位置历史记录（可选功能）
5. ✅ 部署到生产环境

祝测试顺利！ 🚀
